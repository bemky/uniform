<div class="section">
    <h1>
        Tooltip
        <span class="uniformLabel -yellow-60">JS</span>
    </h1>
    <p class="text-xl text-light margin-v-2x">
        Assign a message to show on hover.
    </p>
    
    
    <div class="flex space-h margin-v-2x">
        <div class="">
            <p>
                The Infield Fly Rule
                <span class="uniformLabel -fill cursor-help" id="example-1">?</span>
                is not called in a consistent manner. Just ask the St Louis Cardinals.
            </p>
            <script>
                document.addEventListener('DOMContentLoaded', function () {
                    new window.Uniform.Tooltip({
                        anchor: document.getElementById('example-1'),
                        content: "The umpire's declaration of an infield fly means that the batter is out (and all force plays are removed) regardless of whether the ball is caught. The rule exists to prevent the defense from achieving a double play or triple play by failing to catch a ball that an infielder could catch with ordinary effort."
                    }).render()
                })
            </script>
        </div>
        <div class="flex-fill width-fill overflow-hidden">
            <% code 'javascript', {css_class: 'overflow-y-scroll width-fill'} do %>
                import {Tooltip} from 'uniform';
                new Tooltip({
                    anchor: document.getElementById('example-1'),
                    content: "The umpire's declaration of an infield fly means..."
                })
            <% end %>
        </div>
    </div>
    
    <h3 class="margin-none">Options</h3>
    <div class="border border-gray-30 bg-white rounded">
        <table class="uniformTable align-top-*">
            <thead>
            <tr>
                <th>Option</th>
                <th>Type</th>
                <th>Description</th>
                <th>Default</th>
                <th></th>
            </tr>
            </thead>
            <tr>
                <td class="text-nowrap"><code>anchor</code></td>
                <td class="text-nowrap">Element</td>
                <td>Element to anchor content to.</td>
                <td></td>
                <td class="text-nowrap"><span class="uniformLabel -red">REQUIRED</span></td>
            </tr>
            <tr>
                <td class="text-nowrap"><code>content</code></td>
                <td>HTML</td>
                <td>HTML to render into popover.</td>
                <td></td>
                <td class="text-nowrap"><span class="uniformLabel -red">REQUIRED</span></td>
            </tr>
            <tr>
                <td class="text-nowrap"><code>align</code></td>
                <td>String</td>
                <td>How to align the popover to the anchor <code>left|right|center|#px top|center|bottom|#px</code></td>
                <td class="text-nowrap"> <code>'center bottom'</code></td>
                <td class="text-nowrap"><span class="uniformLabel">OPTIONAL</span></td>
            </tr>
            <tr>
                <td class="text-nowrap"><code>container</code></td>
                <td>Element</td>
                <td>Element to append popover to.</td>
                <td class="text-nowrap"><code>document</code></td>
                <td class="text-nowrap"><span class="uniformLabel">OPTIONAL</span></td>
            </tr>
        </table>
    </div>
</div>